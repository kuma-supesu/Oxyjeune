{% extends "base.html.twig" %}
{% block body %}
    <a class="btn btn-secondary btn text-center" href="{{ path("index") }}" role="button">Retour à l'accueil</a>
    {% for list in planning|sort((a, b) => a.debut <=> b.debut)|reverse %}
        <tbody>
            <td><a href="{{ path("planningInfo", {slug: list.debut|date("d-m-Y") }) }}"><button class="btn-info btn">{{ list.debut|format_date("long",locale="fr") }} - {{ list.event|upper }}</button></a></td>
        </tbody>
    {% endfor %}
    {% block javascripts %}
        {{ parent() }}
    {% endblock %}
{% endblock %}
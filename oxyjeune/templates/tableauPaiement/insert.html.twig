{% extends "base.html.twig" %}
{% block body %}
    <a class="btn btn-secondary btn text-center" href="{{ path("tableauLigneInfo", {tableau: tableau, ligne: ligne }) }}" role="button">Retour à la ligne</a>
    <form method="post">
    {{ form_row(form.dateVersement) }}
    {{ form_row(form.sommeVersement) }}
        <button class="btn btn-secondary" type="submit" id="tableauPaiement_save" name="tableauPaiement[save]">Ajouter</button>
        {{ form_row(form._token) }}
    </form>
{% endblock %}